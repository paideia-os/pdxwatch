# pdxwatch v1.2.0 — release manifest (source form)
#
# This is the human-readable source form of `manifest.pdxsig`. The
# release tool at pkgs.paideia-os mirror-push time (a) enumerates the
# artifact list below, (b) recomputes every BLAKE3-256 hash from the
# repo tree at the v1.2.0 tag, (c) writes the completed manifest into
# a binary `manifest.pdxsig`, and (d) invokes
#   paideia-pq-sign::sign_release_artifact(sk, manifest_path)
# once with the paideia-release-line Ed25519 secret key and once with
# the ML-DSA-65 secret key. The two detached signatures are appended
# to the manifest in the fixed layout below and the resulting
# `manifest.pdxsig` is pushed to
#   https://pkgs.paideia-os/main/pdxwatch/1.2.0/manifest.pdxsig
# alongside the compiled artifacts.
#
# Format mirrors `libpdx-volume`'s `release/manifest.pdxsig.txt`
# (this org's other R53-era satellite through M5) byte-for-byte in
# structure — see that file for the worked template this one follows.
# Every `<BLAKE3-*>` / `<...-KID-*>` / `<...-SIG-*>` slot below is a
# placeholder the release tool fills in from the working tree at tag
# time; NONE are computed by this milestone (see
# release/RELEASE-1.2.0.md §4 S3 for why signing is out of scope
# here).
#
# Reference:
#   design/paideia-as/v0.20-issue-1025-pq-signing.md — Hybrid,
#     sign_release_artifact, HybridSignature (Ed25519 || ML-DSA-65).
#   design/02-development-environment.md §1140            — hybrid
#     release-artifact signature rationale (Ed25519 + ML-DSA-65).
#   design/tooling/plan.md §6.3                           — package
#     repository layout, `manifest.pdxsig` per version directory.
#   design/graphics/r102-user-plan.md §4.8 (paideia-os)   — R102
#     satellite release policy incl. M5-001 scope for pdxwatch.

format-version:      1
manifest-schema:     paideia-manifest-pdxsig@1
package-name:        pdxwatch
package-version:     1.2.0
package-release:     M5-001 (v1.2.0 signed release closer -- M4 witness triplet + release wire-up)
package-license:     MIT
source-repo:         https://github.com/paideia-os/pdxwatch
source-commit:       <TAG-v1.2.0-COMMIT-SHA>   # release tool fills in
source-tag:          v1.2.0

# ---------------------------------------------------------------
# Artifact set — every file the compiled package ships. The hash
# discipline is BLAKE3-256, upper 32 bytes rendered as 64 hex chars.
# The release tool recomputes each hash from the working tree at the
# v1.2.0 tag before writing the binary manifest. The order below is
# stable (lexicographic within each section) so the concatenated
# hash-input is reproducible across signers.
# ---------------------------------------------------------------

[artifacts.source]
caps.decl                          blake3-256 = <BLAKE3-caps.decl>
manifest.pdxproj                   blake3-256 = <BLAKE3-manifest.pdxproj>
src/collectors.pdx                 blake3-256 = <BLAKE3-collectors.pdx>
src/input.pdx                      blake3-256 = <BLAKE3-input.pdx>
src/main.pdx                       blake3-256 = <BLAKE3-main.pdx>
src/widget_cpu.pdx                 blake3-256 = <BLAKE3-widget_cpu.pdx>
src/widget_mem.pdx                 blake3-256 = <BLAKE3-widget_mem.pdx>
src/widget_net.pdx                 blake3-256 = <BLAKE3-widget_net.pdx>

[artifacts.tests]
tests/test_click_cycle_smoke.pdx   blake3-256 = <BLAKE3-test_click_cycle_smoke.pdx>
tests/test_q_quit_smoke.pdx        blake3-256 = <BLAKE3-test_q_quit_smoke.pdx>
tests/test_seeded_stat_smoke.pdx   blake3-256 = <BLAKE3-test_seeded_stat_smoke.pdx>

[artifacts.tools]
tools/build.sh                     blake3-256 = <BLAKE3-build.sh>
tools/release-verify.sh            blake3-256 = <BLAKE3-release-verify.sh>

[artifacts.release]
release/RELEASE-1.2.0.md           blake3-256 = <BLAKE3-RELEASE-1.2.0.md>

[artifacts.legal]
LICENSE                            blake3-256 = <BLAKE3-LICENSE>
README.md                          blake3-256 = <BLAKE3-README.md>
CHANGELOG.md                       blake3-256 = <BLAKE3-CHANGELOG.md>
STATUS.md                          blake3-256 = <BLAKE3-STATUS.md>

# ---------------------------------------------------------------
# Cross-repo dependencies. The consumer's package tool verifies that
# every `depends-on` row is satisfied by an installed package at a
# compatible version. pdxwatch declares three library floors matching
# `manifest.pdxproj` `deps:` (libpdx-gfx / libpdx-event / libpdx-font
# at ^0.1) — these are DECLARED but not LINKED at v1.2.0 (M1-005
# linker landing packages them into the final ELF; until then the
# widget stack + input dispatcher carry inline stubs). Declared here
# so a v1.3+ release that flips the widget stubs off does not have to
# widen this block.
# ---------------------------------------------------------------

[depends-on]
libpdx-gfx     ^0.1     # widget stack (STUBBED at v1.2.0; retires at v1.3.0)
libpdx-event   ^0.1     # input dispatch (STUBBED at v1.2.0; retires at v1.4.0)
libpdx-font    ^0.1     # widget label rendering (STUBBED at v1.2.0)

# ---------------------------------------------------------------
# Substrate contracts — what MUST be present in the runtime for the
# compiled artifact to link and execute correctly. pdxwatch's
# collect_tick path invokes three syscalls directly (sysno 66, 83,
# 115); the M2 widget fingerprint emits reuse sysno 12
# (sys_debug_puts). caps.decl carries the full contract; the row
# below is what the consumer's package tool cross-checks against the
# kernel's dispatch table.
# ---------------------------------------------------------------

[substrate]
paideia-as-toolchain    >= 0.36.0                          # encoder floor
                                                            # (module-basename-pascal,
                                                            # 2-op imul reg,reg,
                                                            # single-line pub let,
                                                            # tightened test-mnemonic).
kernel-syscall          = sys_debug_puts (12)              # widget + input + release
                                                            # fingerprint emits.
kernel-syscall          = sys_clock_read_ns (66)           # collect_tick ns-domain
                                                            # deadline gate.
kernel-syscall          = sys_taskinfo (83)                # collect_tick task-table
                                                            # aggregator.
kernel-syscall          = sys_semantic_send (115)          # v1.1-B SysStatRecord@0.1
                                                            # producer path.

kernel-cap-kind         = KIND_SURFACE                     # M1-002 (mint/commit/revoke;
                                                            # forward-declared -- not
                                                            # exercised at v1.2.0).
kernel-cap-kind         = KIND_IPC_ENDPOINT                # M3-001 (subscribe; libpdx-
                                                            # event.M3 substrate PENDING).
kernel-cap-kind         = KIND_MEMORY                      # M1-002 (mint/revoke; parent
                                                            # cap for KIND_SURFACE
                                                            # derivation).

# ---------------------------------------------------------------
# Emitted output schemas — schemas the compiled package publishes on
# a KIND_SEMANTIC_PIPE. Consumers (monitoring daemons, downstream
# aggregators) filter subscribers on the schema tag below. The wire
# shape is defined in caps.decl `declares_output_schemas`.
# ---------------------------------------------------------------

[emits-schemas]
SysStatRecord@0.1       = 0x41545353    # 'SSTA' LE; 160 bytes = prefix of
                                        # CollectSnapshot[+32..+192]; emitted
                                        # once per collect_tick refresh; see
                                        # caps.decl §Declared output schemas.

# ---------------------------------------------------------------
# Signature block — dual-signed per design/02-development-environment.md
# §1140 (hybrid Ed25519 + ML-DSA-65). Both signatures cover the SAME
# canonical byte-stream: the manifest body above (everything before
# the [signatures] marker line) hashed with BLAKE3-256 and signed via
# paideia-pq-sign::sign_release_artifact. Verification is AND-semantics:
# both signatures MUST verify; failure of either rejects the package.
#
# NOT PERFORMED AT THIS MILESTONE. The paideia-as `mldsa65_sign`
# intrinsic requires a live 32-byte ML-DSA-65 seed key, and this repo
# holds no such key material (paideia-release-line custody --
# hardware-backed TPM 2.0 or cloud KMS per design/02-development-
# environment.md §1164). The signature block below is left as the
# documented placeholder value `SIGNATURE_PLACEHOLDER_PENDING_LIVE_SIGN`
# in every slot the real dual-sign pass would fill; see
# release/RELEASE-1.2.0.md §4 S3 for the full rationale.
# ---------------------------------------------------------------

[signatures]
signer-identity      = paideia-release-line
signer-key-source    = design/02-development-environment.md §1164
                       (hardware-backed in CI -- TPM 2.0 on the
                       release runner, or a cloud KMS pending PQ-KMS
                       availability check per that section's TODO).
signature-scheme     = hybrid-ed25519+ml-dsa-65 (paideia-pq-hybrid-v1)
hash-input-algo      = blake3-256
hash-input-domain    = "paideia-release-artifact\x00" || manifest-body-bytes
                       (matches paideia-pq-sign::sign_release_artifact
                       domain separation, crates/paideia-pq-sign/src/
                       release.rs.)

ed25519-key-id       = SIGNATURE_PLACEHOLDER_PENDING_LIVE_SIGN
ed25519-signature    = SIGNATURE_PLACEHOLDER_PENDING_LIVE_SIGN

ml-dsa-65-key-id     = SIGNATURE_PLACEHOLDER_PENDING_LIVE_SIGN
ml-dsa-65-signature  = SIGNATURE_PLACEHOLDER_PENDING_LIVE_SIGN

# End of manifest.pdxsig source. The release tool ships a canonical
# binary form (`.pdxsig`) alongside this source form; keeping both in
# the repo means a downstream auditor can diff the source-form
# manifest against the binary the mirror served, without needing to
# re-run the hash pass on the repo tree. The dual-sign pass that
# fills in [signatures] above and every `<BLAKE3-*>` placeholder is a
# release-time step performed with live key material outside this
# repo and never checked in.
